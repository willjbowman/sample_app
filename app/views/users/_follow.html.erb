<%= form_for(current_user.relationships.build(followed_id: @user.id), remote: true) do |f| %> <!--remote: true adds Ajax-->
	<div><%= f.hidden_field :followed_id %></div> <!--POSTS :followed_id-->
	<%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>